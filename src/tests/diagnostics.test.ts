import { describe, expect, it } from 'vitest'; import { truncateDiagnosticLabels } from '../content/providers/dom'; import { createSafeDiagnosticCopy } from '../lib/diagnostics'; import type { ProviderDiagnostics } from '../lib/types';
const diagnostics:ProviderDiagnostics={providerId:'gemini',providerName:'Gemini',url:'https://gemini.google.com/app/abc',conversationId:'abc',temporaryConversationKey:'temp:gemini:tab:1',composerFound:true,sendButtonFound:true,stopButtonFound:false,regenerateButtonFound:true,citationSourceRegionFound:true,thinkingBlockFound:true,toolTaskPanelFound:true,terminalOutputPanelFound:false,busyIndicators:['thinking'],readyIndicators:[],errorIndicators:['consent'],latestAssistantMessageFound:true,composerType:'contenteditable',insertionStrategy:'execCommand-insertText',lastCheckedAt:12,experimentalProvider:true,selectorOverridesActive:true,selectorOverrideKeys:['composer'],uncertainReason:null};
describe('provider diagnostics safety',()=>{
  it('caps label count and length',()=>{const r=truncateDiagnosticLabels(Array.from({length:20},(_,i)=>i+':'+'x'.repeat(200)));expect(r).toHaveLength(12);expect(r.every(v=>v.length<=80)).toBe(true);});
  it('copies expanded selector facts without content or real keys',()=>{const safe=createSafeDiagnosticCopy(diagnostics,true,'busy');const text=JSON.stringify(safe);expect(safe.provider).toMatchObject({id:'gemini',conversationType:'persistent',experimental:true});expect(safe.selectors).toMatchObject({terminalOrOutput:false,overrideKeys:['composer']});expect(text).not.toContain('temporaryConversationKey');expect(text).not.toContain('conversationId');expect(text).not.toMatch(/conversationText|assistantResponse|queueItems|promptText/i);});
});
